# 운동학 동적 교체 트랙 — 세션 인계문

**기준 시각 2026-09-15 10:20 KST** · 이전 세션 ID `482389f5-a3cf-44e3-8152-889a296baeb1`

> 새 세션이 이 트랙을 이어받을 때 **가장 먼저 읽는 문서**다.
> 옛 `rsi_track_handover.md` 는 **폐기**됐다 — RSI 30분봉·속도저울은 둘 다 내려갔다.
> 세션 이름이 "RSI Track Handover" 로 보여도 **지금 트랙은 그게 아니다.**
> 보고 형식 정본은 `.claude/references/rsi_report_format.md`. 여기엔 **상태·미결·함정**만 적는다.

---

## 0. 새 세션이 처음 할 일 (순서대로)

1. **이 문서를 끝까지 읽는다.**
2. 상태를 직접 확인한다 — §2 의 한 줄 명령.
3. **`ScheduleWakeup` 을 건다.** 안 걸면 정기 보고가 영영 안 나간다(§5).
   이전 세션은 **10:29 기동 예약**을 남기고 닫혔다 — 세션이 닫히면 그 예약도 죽는다.
4. 대표님께 현재 상태를 한 번 보고하고, 이후 :01/:31 +4분 주기로 올린다.

---

## 1. 지금 무엇이 돌고 있나

**모두 민트에서 돈다.** `ssh mint@183.99.228.81` → `cd /home/mint/auto_trading/backend && source venv/bin/activate`
로컬은 코드 편집용이다. 실거래 조작 스크립트는 **반드시 백엔드 디렉터리에서** 돌린다(`/tmp` 금지).

| PM2 이름 | 정체 | 계좌 | 규칙 |
|---|---|---|---|
| `kinematics-live-s6b240` | 실자금 | **15** · 1x · **nice 0** | **s6양다리** `6 both 0 --hold-min 240 --tag h240` (롱3+숏3) |
| `kinematics-live-anti3s` | 실자금 | **8** · 1x | **역확인3** `--signal impanti` k=15 · 240분 |
| `kine-halt-gate` | 낙폭 문지기 | — | 60초 주기 |
| `kinematics-paper-*` **18갈래** | 대조군·교체 후보 풀 | 없음 | — |
| └ `kinematics-paper-revcont` | **롱 축 발굴** (09-15 신설) | 없음 | `rev` 연속 · 120분 · 10슬롯 양다리 |

공통 규격 — 슬롯 3 · 숏 · 손절 5% · 손절냉각 240분 · 통행료 **왕복 0.1004%**(실측, 명세 0.072% 아님).

**두 규칙은 기계적으로 반대 선별이다** — 방향숏3 은 *떨어지는* 종목을, 역확인3 은 *오르는* 종목을
숏한다. 3시간 토막 손익 상관 **+0.084**. 09-13 에 같은 계열(imp) 두 계좌가 함께 무너진 뒤
**계좌 간 분산을 조건으로 박았다.**

### 🔄 계좌8 교체 — 2026-09-16 11:37 KST · 역확인3 → **확인숏3** (대표님 승인)

**①~⑧ 전부를 통과한 첫 정식 교체.** ⑧ 4/6 + ⑤ 연속 3회.

**판단 근거는 24h 순위가 아니라 꼬리 의존도였다.** 두 갈래 다 상위 5건을 빼면
무너진다(확인숏3 −0.056% · 역확인3 −0.351%). 갈린 것은 **최대 1건만 뺐을 때**다 —
확인숏3 거래당 +0.251%(통행료 2.50배)인데 역확인3는 +0.089%(**0.88배**)로 이미
제 통행료를 못 냈다. 여섯 축(자본%·거래당·꼬리·4토막·집중도·손절율) 전부 우위.

| 단계 | 결과 |
|---|---|
| 0. 강제 청산 3건 | IDOL +8.91% · SYN +5.69% · CTR +1.89% → 지갑 385.24 → **405.51** (+20.27) |
| 엔진 정지 | 청산 **직후** `pm2 stop` — 순서를 바꾸면 재진입한다 |
| 1. 인자 일치 | 페이퍼 `conf3s` 와 세 곳만 다름(`impconf` · `--tag conf` · `--dir conf3s`) |
| 2·3. 새 원장 | `runs/kinematics_live/conf3s` · PEAK 405.51 |
| 4. 네 파일 | `_rotate.LIVE` · `_regime.TWINS` · `_accel.LIVE` · `kine_halt_gate.ACCTS` |
| 5. 기동 | `KINE_NICE=0` · 슬롯 3 · 다리당 $128.41 · `pm2 save` |
| 6. 문지기 | 재기동 후 「계좌8 확인숏3 열림 낙폭 −0.04%」 눈으로 확인 |
| 7. 네 스크립트 | 전부 exit 0 · `_accel` 이 새 원장(0건·표본부족) 읽는 것 확인 |

역확인3는 **후보 풀에 남긴다** — 내려온 갈래도 다시 올라올 수 있다.
민트 커밋 `0a1f6203`.

### 🔄 계좌15 교체 — 2026-09-15 15:25 KST · 방향숏3 → **s6양다리** (대표님 승인)

**계좌15 는 이제 롱3+숏3 양다리다.** 실거래에서 롱이 나가는 첫 트랙이다.

- 교체 근거 — s6양다리가 조건 ①~⑥ 전부 + ⑤ 연속 2/2 충족.
  12h +4.18(vs −5.07) · 24h +11.12(vs +1.92) · 상관 −0.122 · **손절율 3%(vs 48%)** ·
  집중도 +4.45(vs −12.96) · 지속성 3/4(vs 1/4). 6일 자본% +24.92 로 전 갈래 1위(222건).
- 방향숏3 최종 성적 — **원장 11건 · 자본 0.9377 → 0.9499** (강제청산 3건 반영).
- **보유 3건 강제 청산**(15:17, 대표님 지시) — "선별이 틀렸다고 교체하면서 그 선별이
  고른 자리를 만기까지 들고 있는 건 앞뒤가 안 맞는다". KOMA −0.33% · RED +3.23% ·
  UAI +1.27%, 지갑 356.03 → **360.58(+4.55)**.
  ⚠ 만기 전 청산이라 엔진이 `exch_gone` 으로 보고 **셋 다 손절로 라벨**했다 —
  손익은 실제 체결가라 정확하고 **라벨만 틀리다.** 옛 원장 손절율을 읽을 때 주의.
  ⚠ KOMAUSDT 는 평가 +0.10% 였는데 체결이 **−0.33%** 였다(슬리피지 0.43%p).
  유동성 얕은 종목의 시장가 청산 비용은 평가액에 안 보인다.
- **증거금이 교체의 선행조건이다** — 보유 3건이 증거금 341.42 를 묶어 가용이 18.74
  뿐이었다. 그대로 교체했으면 6건이 전부 `-2019` 로 거절된다(필요 345.35).
  **교체 절차에 「보유 0 확인」을 0단계로 넣는다.**
- 네 곳 동기화 — `_rotate.py:89` · `_regime.py:36` · `_accel.py:27` ·
  `kine_halt_gate.py:74`. 백업 `/home/mint/cutover.bak/`.
- `PEAK.json` = **360.58**(교체 시점 지갑) · 정지 문턱 **324.52**.
- **`KINE_NICE=0` 로 띄웠다** — 그동안 두 실거래가 페이퍼와 같은 **nice 15** 로
  돌고 있었다(교훈#102 체크리스트 9 위반). 런처에 경고가 적혀 있는데 배선이
  빠져 있었다. **⚠ 계좌8(anti3s)은 아직 nice 15 다 — 재기동 필요, 미승인.**
- 첫 사이클(15:26) 검증 — **롱 3건(BEAT·BSV·WAXP) 숏 3건(ACU·EGLD·ZORA)** ·
  거래소 6/6 · 무보호 0 · 손절 방향 정확(롱 SELL 아래 · 숏 BUY 위) ·
  건당 명목 $55.6~57.0 · 문지기가 새 이름으로 열림 인식.
- **정지 해제는 자동으로 됐다** — 새 원장 디렉터리에 `HALT_ENTRY` 가 없기 때문이다.
  옛 `dir3s/HALT_ENTRY` 는 남아 있으나 아무도 안 읽는다.

### 🛑 (종료) 계좌15 진입 정지 — 2026-09-15 14:46~15:25 KST

**손으로 `HALT_ENTRY` 를 걸었다.** 문지기의 −10% 자동 정지가 아니라 **수동**이다.

- 근거 — 낙폭 −6.03%(경고선 −7% 까지 0.97%p) · 실거래 **8건 중 5건 손절(63%)** ·
  실현합 **−19.24%p** · 24h 자본% +6.05 → **+0.22**(죽은 토막이 빠지며 실체 노출) ·
  잠식 **82%** · 집중도 **−14.66** · ⑥ 경고 **다섯 주기 연속** 1/4.
  그리고 **교체가 아직 불가능**했다 — s6양다리는 연속 1/2 회였고, 양다리라
  **롱 경로가 실거래에서 한 번도 안 나가봤다**.
- 검증한 것(교훈#88·#100 — 두 경로를 다 확인) —
  ① 문지기는 `MANUAL_RESUME_ONLY=True` 라 `unlink()` 분기에 **안 들어간다**
  (수동 정지가 안 지워진다) ② 엔진 로그 **14:46:30 `🛑 신규 진입 정지 중 —
  후보 515 · 대기 0 를 버린다`** ③ 상태 한 줄이 `정지` 로 바뀜 ④ 1분 뒤에도 파일 존속.
- 막힌 것 — 신규 진입 · 대기열 승격. **안 막힌 것 — 손절 · 만기 청산 ·
  거래소 대조 · 보호 점검.** 보유 3건(KOMA 17:10 · RED 17:55 · UAI 18:20)은 정상 청산된다.
- **재개는 대표님 승인 후** `rm runs/kinematics_live/dir3s/HALT_ENTRY` → 1분 내 반영.
  ⚠ 낙폭이 −10% 문턱 아래로 가면 손으로 지워도 문지기가 1분 안에 되돌린다.
- **계좌8 은 정지하지 않았다** — 낙폭 −0.01% · 손절 0건 · 지속성 3/4.

### 상태 스냅숏 (2026-09-15 10:17)

| | 계좌15 방향숏3 | 계좌8 역확인3 |
|---|---:|---:|
| 지갑 | **366.08** | **366.94** |
| 전고점(`PEAK.json`) | 378.89 | 367.16 |
| 낙폭 | −3.38% | −0.06% |
| **정지 문턱 지갑** | **341.00** | **330.45** |
| 상태 | 🟢 열림 | 🟢 열림 |
| 거래소/엔진 | 3/3 · 3 | 3/3 · 3 |
| 새 원장 | 2거래 · 실현 −10.35% · **손절 2** | 0거래 |

**합산 733.02** (원금 800 대비 −8.37%)

⚠ **계좌15 를 주시하라.** 교체 직후 첫 두 거래가 모두 손절이다. 방향숏3 의 손절율 43% 는
교체 때 이미 짚은 약점이다. **낙폭이 −7% 를 넘으면 보고 ①항에 경고로 올린다.**

---

## 2. 상태 확인 한 줄

```bash
ssh mint@183.99.228.81 'cd /home/mint/auto_trading/backend && source venv/bin/activate && python3 - <<"PY"
import json, os
from scripts.binance.kine_live_broker import KineLiveBroker
for aid,dr,nm in ((15,"dir3s","계좌15 방향숏3"),(8,"anti3s","계좌8 역확인3")):
    b=KineLiveBroker(account_id=aid,leverage=1); b.connect()
    w=b.wallet_balance(); pos=b.positions() or {}; alg=b.open_algo_orders() or {}
    pk=json.load(open(f"runs/kinematics_live/{dr}/PEAK.json"))["peak"]
    halt=os.path.exists(f"runs/kinematics_live/{dr}/HALT_ENTRY")
    print(f"{nm} 지갑 {w:.2f} 낙폭 {100*(w/pk-1):+.2f}% 문턱 {pk*0.9:.2f} "
          f"{'정지' if halt else '열림'} 거래소 {len(pos)}/{len(alg)} 무보호 {sorted(set(pos)-set(alg)) or '없음'}")
PY'
```

---

## 3. 운용 방식 — 동적 전략 교체

2026-09-15 대표님 지시로 **고정 전략 + 장기 검정**에서 **동적 교체**로 바뀌었다.

> "시시각각 국면과 각 전략의 상승세를 파악해 동적으로 전략을 적용한다.
>  아주 기민하고 과감하게 생각하고 행동해야 해."
> "아카이브 6년 검증보다 지금 상승세를 주목해줘. 매번 과거 데이터 돌리는 것이
>  무슨 의미가 있어? 국면이 변경되면 또 다시 바꾸면 되는 거고."

- 근거는 **최근 창 실측**(3h/6h/12h/24h/48h)뿐.
  **아카이브 6년 검정·최대통계량 귀무는 먼저 꺼내지 않는다.** 대표님이 물으면 답한다.
- **관측만 하고 넘기지 않는다** — 매 정기 보고에 갈래 순위와 **교체 결론**을 낸다.
- 약점은 감추지 않고 같이 올린다 — 집중도 음수(소수 대박), 손절율, 나쁜 날 성적.
- 메모리 `feedback_dynamic_strategy_rotation.md` 에 같은 내용이 있다.

### 도구 셋 (전부 `backend/` 최상위)

| 파일 | 하는 일 |
|---|---|
| `_rotate.py` | **갈래 순위 + 계좌별 교체 판정.** 조건은 파일 머리에 못 박혀 있다. ⚠ **「잠식」 열은 낙폭이 아니다** — `100×통행료÷(거래당 net+통행료)`, 즉 **통행료가 그로스 엣지를 먹는 비율**이다. 100% 초과 = 수수료 전으로도 제 통행료를 못 냄. `—` 는 그로스≤0 이라 계산 불가 |
| `_regime.py` | 분위기 점검 + **판정 원장 사후 채점**. 정지된 계좌의 **재개 시점**을 잰다 |
| `_accel.py` | 손익 가속도 · **엣지기울기 정본 산식**. 실거래 원장은 `LIVE` 등록표. **갈래별 슬롯 수를 `BR` 세 번째 칸에 들고 있다** |
| `_dir.py` | **국면 방향** — 거래소 원본으로 무조건 보유 수익률. 갈래로 역산하지 마라 |
| `_vs.py` | **두 갈래 국면 대조** — 창별 거래당 엣지·체결 수·최대1제외. 창 길이로 순위가 뒤집히는지 본다 |
| `_mfe.py` | **보유 되돌림 추적** — 최고·되돌림·σ배수. 트레일링 판단 재료(주문 안 냄) |
| `kine_trail.py` | **트레일링 스톱** — 손절선을 올리기만 한다(래칫). 대역 = 15분 σ × 2 |
| `_trend.py` | **추세 진단** — 비겹침 3h 토막으로 '강해지는 갈래' 를 줄 세운다 |
| `_common*.py`·`_overlap.py`·`_vol.py`·`_stop.py`·`_regime2.py`·`_longleg.py` | **공통 성분 진단 6종** (2026-09-15). 전부 읽기 전용 |
| `scripts/binance/paper_ab.py` | 페이퍼 A/B 전 갈래 (**갈래 이름의 정본 등록표**) |

### 공통 성분 — 갈래 성적의 대부분은 그 갈래가 아니다

2026-09-15 대표님 질문("왜 다 같이 지나")에서 나온 실측이다.

- 횡단면 평균 토막수익이 **앞 25토막 +0.542 → 뒤 25토막 −0.136**,
  아홉 중 **여덟**이 같은 방향으로 나빠졌다. 가속도가 전부 음수인 건
  갈래가 안 갈려서가 아니라 **앞이 좋고 뒤가 나쁜 기간 구조** 때문이다.
- **기각된 설명 네 개** — 다시 파지 말 것:
  ① BTC 방향 (공통성분 상관 **−0.009**) ② 종목 겹침 (자카드 중앙 **0.166**)
  ③ 알트 진폭 (토막상관 −0.108. 진폭은 뒤 절반에 **+81%** 늘었는데 성적은 나빠짐)
  ④ 손절 5% (손절율 15.9%→24.4% 로 그럴듯했으나 **손절 없는 무손숏3 이
  −2.144 로 나머지 8종 −0.624 보다 3.4배 더 무너져 반증**)
- 남은 설명 — **알트 전반이 뒤 절반에 +19.36% 올랐다**(앞 +7.92%). 아홉 갈래가
  사실상 **전부 숏**이라 공통으로 불리하다. 되돌림 자기상관은 오히려 강해져
  (−0.010 → −0.034) '추세 지속' 설명은 기각. **근거는 구간 2개 비교뿐이라 약하다.**
- **상관이 낮은 것과 같이 지는 것은 모순이 아니다** — 상관은 평균을 빼고 잰다.
  방향숏3↔역확인3 토막상관 +0.084 인데도 평균이 같이 끌려간다.
- 그래서 **⑤항에 「초과」 열**을 박았다(24h 자본% − 전 갈래 24h 중앙값).
  평균이 아니라 **중앙값**을 쓴다 — 충격60(−28%) 한 갈래가 평균을 끌어
  전 갈래를 실제보다 좋아 보이게 만든다.

### ✅ 롱 경로 실거래 검증 완료 — 2026-09-15 14:51 KST (대표님 승인 소액 실주문)

**롱은 실거래에서 끝까지 열린다.** 두 계좌가 그동안 `3 short 0` 으로만 돌아
한 번도 확인된 적이 없던 구간이다. s6양다리(롱3+숏3)를 올리려면 필요했다.

| 단계 | 결과 |
|---|---|
| 롱 진입 | `Futures BUY DOGEUSDT qty=72 status=FILLED` · 0.08316 · 명목 $5.99 |
| `positions()` | **+72.0 (양수)** — 롱으로 정상 인식 |
| 손절 등록 | **SELL** 트리거 **0.079** (진입 −5%, tick 반올림) · algoId 발급 |
| 보호 점검 | 무보호 **없음** |
| 청산 | 0.08315 · 사후 포지션·조건부주문 **잔여 0** |
| **실측 왕복 수수료** | **0.0998%** — 숏 기준 0.1004% 와 사실상 같다 |

총비용 0.112%($0.0067). `DOGEUSDT` 레버리지가 계좌15에 1x 로 설정됐다(트랙 설정과 동일).

**dry_run 으로는 이 구간을 못 연다** — 어댑터를 아예 안 부른다.
그리고 `--live --dry-run` 으로 엔진을 통째로 돌리면 **`broker.notify` 가
dry_run 게이트를 안 타서 가짜 진입 알림이 텔레그램으로 나간다**(정지 중에
체결이 난 것처럼 보인다). 그래서 브로커 클래스를 직접 태웠다 — `_longcheck.py`.

⚠ **정정** — `kine_live_broker.py` 머리의 "`q > 0` 만 담고 손절은 `side="SELL"`
하드코딩" 경고는 **`rsi_live_broker`(계좌8의 옛 RSI 브로커)** 얘기다.
`kine_live_broker` 는 **그 한계 때문에 양다리용으로 새로 만든 계층**이다.

### 롱 축 — imp 거울은 닫혔다 (2026-09-15)

`kinematics_paper.py` 는 imp 후보를 z_vel(=−imp)로 정렬해 **절반 롱 / 절반 숏**
으로 가른다(숏=imp 최저, 롱=imp 최고). 즉 **롱은 별도 가설이 아니라 숏의 거울**이다.
충격스프3 실측 — 롱 125건 거래당 **+0.055 (t +0.31 · 95%하한 −0.298)**,
숏 163건 **+1.219 (t +1.97 · 하한 +0.004)**. 손절 제외 만기분만 보면
**롱 +0.182 (t +0.97) vs 숏 +5.153 (t +7.54 · 승률 73.1%)**.
**뒤 절반에 알트가 +19.36% 올랐는데도 롱은 −0.084** 였다.
→ 충격스프3 은 **충격숏3 + 엣지 0 인 롱**이다(자본% +9.72 vs 충격숏3 +22.38).
**분산이 아니라 희석이다.** 롱 축이 필요하다는 방향은 유효하나 **imp 거울로는 안 된다.**

### 롱 축 발굴 — 진행 중 (2026-09-15 대표님 승인)

**0단계 대조군 · 검정력** (`_longbase.py`)

- **무조건 롱 대조군** — 선별 없이 그냥 사서 들기(25종목 · 15분봉 · 통행료 차감,
  09-09 11:00~). 240분 **+0.615**(t +2.24) · 480분 **+1.330**(t +2.42).
  ⚠ 중앙값은 전부 음수이고 이 구간이 알트 +19.36% 였다 — **하락 구간이면 부호가
  뒤집힌다.** 롱 후보는 **절대 수치가 아니라 같은 구간 대조 대비 초과분**으로만 판정한다.
- **검정력** — 롱 거래당 SD **2.02** vs 숏 **7.92**. +0.50%/거래를 잡는 데
  롱 **128건(7일)** · 숏 1,967건(109일). **롱 축은 검정이 15배 싸다.**
  숏 분포가 험한 건 −5% 손절 절단 + 오른쪽 꼬리 +35.92 때문이다.

**⛔ `sess`(아시아 세션 이월) — 기각.** 코드 주석의 "샤프 0.77~1.04 · 연 +42%"를
믿고 후보로 올렸다가 원본을 읽고 철회했다. `runs/research_track/session_2026_08_31/crosswin.csv`
(758일 IS + 715일 OOS = 1,473일) 실측 — 상위롱 3종목 **IS 날짜t +1.873 / OOS +0.348**,
**OOS 상위 5일을 빼면 −0.068**(그 5일이 238%). 종목 수를 늘리면 단조 악화
(3→5→10: OOS t +0.348 → −0.075 → −1.078). 격자가 **162칸**인데 IS 최고가 1.873 이다.
주석의 "+42%"는 **추세스프레드 5종목 IS 값**이고 같은 칸 OOS 는 +3.9%(t 0.214).
**교훈 — 주석이 아니라 원본 파일을 읽어라.**

**⛔ 기존 롱 다리 5종 — 전부 무조건매수에 진다** (`_longsurvey.py`)

| 갈래 | 신호 | 롱거래 | 롱 net | 초과 |
|---|---|---:|---:|---:|
| s6양다리 | kine | 114 | +0.319 | −0.296 |
| s2양다리 | kine | 74 | −0.008 | −0.623 |
| s10양다리 | kine | 371 | −0.091 | −0.706 |
| 체결자기여기 | ac1 | 54 | +0.144 | −1.186 |
| 충격스프3 | imp | 56 | +0.028 | −1.302 |

**🔍 유일한 양수 — `rev`(1시간 되돌림) 롱.** `s10both_d5_utc01`(UTC 1시 진입 ·
5분 지연 · 120분 보유 · 10슬롯 양다리, 09-01~09-15 150건) —
**롱 75건 +0.675(t +1.87 · 중앙 +0.264 · 승률 53.3%) · 숏 75건 −0.010.**
무조건 롱(120분 +0.219) 대비 **초과 +0.456%p**. 롱만 벌고 숏은 0 —
교훈#91 이 요구하는 비대칭이 제대로 나왔다.

**▶ 가동 중 — `kinematics-paper-revcont`** (2026-09-15 13:33 기동).
utc01 에서 **`--entry-hour 1` 하나만 뺐다** — 다른 인자는 전부 동일하다.
`10 both 5 --signal rev --hold-min 120 --tag revcont --stop-cooldown-min 120`
원장 `runs/kinematics_paper/s10both_d5_revcont`.
**가르는 질문** — utc01 의 +0.675 가 (가) `rev` 신호 때문인가 (나) UTC 1시라는
**시각** 때문인가. 교훈#85(펀딩 정산은 시간대 효과)가 정확히 이 함정이다.
재현되면 신호, 죽으면 시각이고 그때는 시각 위약(같은 신호·다른 시각 12칸)으로 간다.
검정력 — utc01 롱 실측 SD 3.13 이라 초과 +0.456%p 를 잡는 데 **369거래**.
하루 한 번이면 74일, **연속 진입(롱 5슬롯 · 120분)이면 약 10일**.
배선 확인(교훈#88) — 첫 대기 10건에서 롱 z_vel −10.4~−4.3 / 숏 +7.9~+2.7,
`z_vel = -rev` 이므로 **롱 = 1시간에 많이 떨어진 것 · 숏 = 많이 오른 것.** 정상.

### 후보 풀 — 여기 없으면 아무리 좋아도 안 올라온다 (2026-09-15 확장)

`_rotate.py` 의 `BR` 이 **곧 후보 풀**이다. 09-15 오후까지 9갈래뿐이었고,
그 밖에서 **s6양다리(`s6both_h240`)가 조건 ①~⑥ 을 전부 통과한 채 몇 시간**
돌고 있었다. 보고는 계속 "교체 후보 없음" 이었다 — **조건은 제대로 돌았고
입력이 빠져 있었다**(교훈#88 계열).

**고를 때 하나만 넣지 마라.** 풀 밖 7갈래를 전부 걸어 그중 하나만 통과하게
두는 것이 "값을 보고 골랐다"를 피하는 방법이다. 실제로 7 중 1만 통과했다.

풀 확장 후 16갈래. 추가분 — `s6both_h240`(s6양다리) · `s2both`(s2양다리) ·
`s10both`(s10양다리) · `s6both_ac1`(체결자기여기) · `s3short_rmz`(최장연속숏) ·
`s10both_d5_utc01`(되돌림UTC1) · `s10both_d5_revcont`(되돌림연속).

⚠ **이름은 `paper_ab.py` 의 `TRACKS` 와 같은 것을 쓴다.** 두 곳이 갈리면
보고가 엉뚱한 갈래를 가리킨다(§6 함정). 09-15 에 두 파일을 함께 고쳤다.
⚠ **공통 성분(중앙값)이 풀 크기에 따라 움직인다.** 9갈래일 때 −4.63% 였던
것이 16갈래에서 **−0.41%** 가 됐다. 「초과」 열의 기준선이 바뀐 것이지
갈래가 나아진 게 아니다. 풀을 또 바꾸면 이 값도 다시 움직인다.
⚠ 갓 띄운 갈래는 `trades.csv` 가 없다. `load()` 가 빈 표를 돌려주게 고쳤다 —
안 그러면 **순위표가 통째로 안 나온다.**

### 교체 조건 (`_rotate.py` 에 고정 — 값을 보고 고치지 않는다)

① 후보의 12h·24h 자본%가 **둘 다** 현행보다 높다 · ② 후보의 24h 자본% > 0 ·
③ 후보와 **상대 계좌** 규칙의 3시간 토막 상관 < +0.50 · ④ 후보 24h 손절율 ≤ 45% ·
⑤ ①~④ 가 **연속 2회(=1시간)** 유지 ·
⑥ **지속성** — 최근 4토막 중 플러스 ≥ 3 **且** 최대 토막 제외 합 > 0
(2026-09-15 대표님 승인 신설) ·
⑦ **두 계좌가 같은 갈래로 가지 않는다** (2026-09-15 22:41 대표님 승인 신설) ·
⑧ **후보 안정성** — 최근 6회차 중 같은 후보가 4회 이상 1위
(2026-09-16 07:49 대표님 승인 신설) ·
⑨ **마찰 관문** — 후보의 **24h 창**에서 「최대 1건을 뺀 거래당 엣지」가
통행료(0.1004%)를 넘어야 한다 (2026-09-16 13:50 대표님 승인 신설)

**⑥ 을 왜 넣었나** — ①~⑤ 는 **수준(누적)만** 본다. 누적이 한 토막에서 나왔는지
꾸준히 쌓인 것인지 구분하지 않는다. 09-15 방향숏3 의 24h +10.26% 는 09-15 00:00
한 토막(+12.05)의 잔상이었고 그 뒤 세 토막은 하나도 플러스가 아니었다
(최대 토막 제외 합 **−3.64**). ⑥ 은 그런 갈래에 실자금을 넘기지 않기 위한
**안전장치**다 — "기울기가 미래 수익을 예측한다"는 주장이 **아니다**
(그 축은 아카이브 6년 43,764거래에서 p 0.8450 로 기각됐다, §8).

**⑥ 은 격자 3개(0/−1h/−2h)의 중앙값으로 잰다.** 토막 경계를 한 시간만 밀어도
판정이 뒤집힌다 — 09-15 실측에서 역확인3 이 **2/4 탈락 → 3/4 → 4/4 통과**로
움직였다(교훈#109 슬롯 타이밍 운). '전 격자 통과'(최솟값)도 검토했으나
3슬롯·3시간 토막의 잡음에서는 어떤 후보도 못 넘어 **교체가 영구히 언다.**

**현행 규칙 경고** — 교체 조건이 아니라 표식이다. 현행의 최근 4토막 중 플러스가
**≤ 1** 이면 `_rotate.py` 가 ⚠ 를 찍는다. 대안이 없어 유지하더라도
**대표님이 그 사실을 알고 유지하셔야** 한다. 보고 ①항에 올린다.

**⑥ 도입이 그날 순위를 바꾸지 않았다** — 조건 ②(24h>0)를 통과하는 갈래가
방향숏3·역확인3 둘뿐이고 그 둘이 이미 두 계좌에 하나씩 들어가 있어,
넣기 전과 후의 판정이 똑같이 '양 계좌 유지'였다. **값을 보고 조건을 고친 게
아니라는 근거**로 이 사실을 남긴다.

**⑦ 을 왜 넣었나** — ①~⑥ 은 계좌마다 따로 돈다. 기존 제외는 `t == other` 로
**상대가 지금 쓰는** 갈래만 막았을 뿐, 같은 주기에 **양쪽이 동시에 옮겨가려는
새 갈래**는 아무도 안 막고 있었다. 09-15 22:31 에 야간 감시관이 올린 교체 건의가
**두 계좌 모두 체결자기여기**였다. 그대로 따르면 조건③(상대 계좌와 상관 < +0.50)이
**잴 대상을 잃고** 계좌를 둘로 나눈 설계가 통째로 무너진다. 값을 보고 만든 조건이
아니라 **처음부터 있었어야 할 구조 제약**이다.

이득(gain = 12h 차 + 24h 차)이 큰 쪽이 갈래를 갖고, 진 쪽은 **다음 후보로
내려간다**(없으면 현행 유지). ⑦ 에서 걸린 갈래는 조용히 버리지 않고 사유를 찍는다
— ⑥ 과 같은 원칙이다.

**⑦ 도입 직후 실측** — 계좌15 는 체결자기여기를 ⑦ 로 놓치고 현행 s6양다리 유지,
계좌8 은 이득이 커(+12.46 vs +10.53) 후보를 지켰다. 커밋 `2a2f16c2`.

**⑧ 을 왜 넣었나** — 09-15 밤 16회차 실측에서 **1위가 거의 매 회차 바뀌었다**:
방향숏3 → 체결자기여기 → 충격240냉 → 확인숏3 → 충격60. 그러면 ⑤ 연속 2회는
**우연히** 채워진다. 조건대로 따랐다면 밤새 4~5번 교체했고 매번 왕복 통행료
0.1004% 와 미실현 포기를 물었다. 24h 창으로 16갈래 중 1위를 고르는 것은
국면 읽기가 아니라 **잡음 좇기**일 수 있다(교훈#95 최대통계량).
⑧ 은 값을 보고 만든 문턱이 아니라 **선택 절차가 잡음을 좇는지 보는 검사**다.

**⑧ 의 이력은 정기 주기에만 쌓는다** (`MIN_GAP_S = 1200`). 보고하려고 손으로
한 번 더 돌린 실행이 이력에 들어가면 ⑤ 연속도 ⑧ 안정성도 부풀려진다 —
09-16 07:47 의 계좌8 "연속 6회" 에 내 수동 실행이 섞여 있었다. 수동 실행은
읽기만 하고 기록하지 않으며, 출력에 그 사실을 찍는다.

⚠ 신설 직후 **최소 4회차(2시간)** 는 어떤 교체도 못 한다 — 이력이 비어 있기
때문이다. 의도된 보수성이다. 커밋 `f98b7d5d`.

**⑨ 를 왜 넣었나** — ①~⑧ 어디에도 **마찰 검사가 없었다.** 09-16 하루에 두 번
(체결자기여기 · 충격60) 수수료를 못 넘는 갈래가 후보 1위로 올라왔다.

**왜 생애가 아니라 24h 인가** — 처음엔 생애 거래당 엣지로 건의했다가
**트랙 원칙("아카이브보다 지금 상승세")과 충돌한다**는 대표님 지적을 받고 고쳤다.
생애로 거르면 국면 적합 갈래를 놓친다.

**왜 최대 1건을 빼나** — 09-16 실측에서 충격60 의 3h 거래당 +0.970%(통행료 9.66배)
가 **한 건을 빼면 −1.378%(−13.72배)** 였다. 한 건이 만든 숫자로 실자금을 옮기지 않는다.

**⑨ 로도 못 막는 것** — 충격60 은 ⑨ 를 통과한다(24h 최대1제외 2.51배). 그런데
**48h 로 넓히면 −15.54%** 이고 **생애 배수 0.47 vs 24h 배수 5.10 — 괴리 10.9배**다.
s6양다리는 그 괴리가 1.03배(4.36 vs 4.51)다. 지금은 이 판단을 조건으로 굳히지 않고
**보고에서 사람이 읽는다** — 창 길이를 바꾸면 순위가 뒤집히는 국면이라, 여기서
문턱을 하나 더 만들면 그것이야말로 값을 보고 만드는 조건이 된다.

**도구** — `_vs.py <tag> <이름> <슬롯> …` 로 두 갈래를 3/6/12/24/48h 창별로
세운다. 창마다 체결 수를 같이 내므로 **좋은 숫자가 몇 건에서 나왔는지** 보인다.
민트 커밋 `6a6ae669`.

### 교체 절차 (2026-09-15 확립 — 이 순서를 지켜라)

1. 페이퍼 쌍과 **인자를 정확히 일치**시킨다(만기냉각 유무까지). 어긋나면 쌍이 딴 규칙이 된다.
2. **새 원장 디렉터리**를 판다 — 규칙을 섞으면 엣지기울기·자본%·집중도가 혼합된다.
3. `PEAK.json` 전고점을 **교체 시점 지갑**으로 재설정한다.
   낙폭 관문은 *현행 규칙이 낸 손실*을 재는 장치다. 옛 손실을 물리면 시작하자마자 정지한다.
0. **보유 0 · 조건부주문 0 을 먼저 확인한다** (2026-09-15 추가). 보유가 있으면
   증거금이 묶여 신규 진입이 전부 `-2019` 로 거절된다 — 실측 가용 18.74 vs 필요 345.35.
   만기까지 기다리거나 **강제 청산**하고 시작한다.
4. **다섯 곳을 함께 고친다** — `_rotate.py` 의 `LIVE` · `_regime.py` 의 `TWINS` ·
   `kine_halt_gate.py` 의 `ACCTS` · `_accel.py` 의 `LIVE` · `_mfe.py` 의 `ACCTS`.
   ⚠ **이 목록은 낡는다.** 09-16 11:37 계좌8 교체에서 `_mfe.py`(그날 아침 신설)를
   빠뜨려 **이미 청산된 포지션을 그대로 보고 자료로 냈다**(SYNUSDT 남은분 −13분).
   고치기 전에 `grep -rn '<옛 원장 디렉터리>' backend/*.py backend/scripts/binance/*.py`
   로 **직접 훑어라** — 목록을 믿지 마라.
   **09-15 에 `_regime.py` 만 안 고쳐 없는 규칙으로 판정했다.**
   같은 날 `_accel.py` 도 빠져, 보고의 "실거래" 줄이 **이미 내려간 충격240
   원장(imp3s·120건)** 을 계속 읽었다 — 09-15 10:30 수정, 표본부족 표시 추가.
5. `pm2 delete` → `pm2 start` (인자 전문을 그대로 읽어 신호 부분만 교체) → `pm2 save`.
   ⚠ **실거래는 `KINE_NICE=0` 으로 띄운다**(교훈#102 체크리스트 9). 09-15 까지
     두 실거래가 페이퍼 17갈래와 같은 **nice 15** 로 돌고 있었다 — 런처에 경고가
     적혀 있는데 배선이 빠져 있었다. 계좌15 는 09-15 교체 때 0 으로 올렸고,
     **계좌8(anti3s)은 아직 15 다.**
6. 문지기 재기동 후 **열림/정지와 보호 점검을 눈으로 확인**한다.
7. **네 스크립트를 전부 실행해 예외 없이 도는지 확인한다** (2026-09-15 신설).
   `_rotate.py` · `_regime.py` · `_accel.py` · `scripts/binance/paper_ab.py`.
   ⚠ "네 곳 수정"으로는 부족하다 — **한 파일 안에 참조가 둘 이상**일 수 있다.
     09-15 15:25 교체에서 `_regime.py` 의 `TWINS` 만 고치고 같은 파일의 `LED` 를
     안 고쳐 **판정이 `KeyError` 로 통째로 죽었다**. 실행해 봤으면 즉시 알았다.

### ⚠ 슬롯 수는 갈래마다 다르다 — 상수로 두지 마라 (2026-09-15)

자본% = 실현합 ÷ **그 갈래의 슬롯 수**다. `_accel.py` 에 `SL = 3` 이 박혀 있어
09-15 15:22~21:10 동안 슬롯 6인 **s6양다리(계좌15 실거래)의 자본%가 정확히
2배**로 출력됐다(+1.24% 로 찍혔으나 실제 +0.62%). 매 보고마다 손으로 고쳤다.
지금은 `BR = [(태그, 이름, 슬롯), …]` · `LIVE = {디렉터리: (계좌, 이름, 슬롯)}`.
**슬롯이 다른 갈래를 새로 들일 때 이 칸을 반드시 채운다.**

---

## 4. 안전 장치

### 낙폭 문지기 `scripts/binance/kine_halt_gate.py`

- **전고점 대비 낙폭 ≤ −10% 면 자동 정지**(신규 진입만 막는다).
- **자동 재개 — 2026-09-15 22:17 대표님 지시로 다시 켰다** (`MANUAL_RESUME_ONLY = False`).
  낙폭이 **−3% 위로 회복되면 문지기가 `HALT_ENTRY` 를 지우고 신규 진입을 다시 연다.**
  (09-14 에는 껐었다 — 그날 기울기·최근N 관문이 차례로 기각돼 "자동으로 다시 켤
  조건을 세울 수 없다"가 이유였다. 그 판단이 뒤집힌 것이다.)
  ⚠ **사람이 건 정지는 풀지 않는다.** 문지기는 파일에 `문지기 자동 정지` 라는
  표식(`GATE_MARK`)이 있는 것만 지운다. 손으로 `touch` 한 정지(09-15 14:46 같은)는
  낙폭이 회복돼도 **유지되고 🔒 텔레그램으로 알린다** — 사람의 판단은 낙폭으로
  되돌릴 수 있는 것이 아니다. 손으로 건 것은 손으로 푼다.
  ⚠ 자동 재개가 일어나면 **🟢 텔레그램**이 온다. 기동 로그에
  `재개 **자동** (문턱 -3.0%)` 과 `⚠ 자동 재개 **켜짐**` 두 줄이 찍히는지 본다.
- 전고점은 `runs/kinematics_live/<dir>/PEAK.json` 에 **영속**한다.
  메모리에만 두면 재기동이 전고점을 오늘로 리셋해 관문이 통째로 풀린다.
- 막는 것: 신규 진입 · 대기열 승격
  **안 막는 것: 손절 · 만기 청산 · 거래소 대조 · 보호 점검**
- 손으로 `HALT_ENTRY` 를 지워도 **낙폭이 문턱 아래면 1분 안에 되돌린다.**
  문지기를 끄려면 `pm2 stop kine-halt-gate` 가 먼저다.
- **텔레그램 알림 (2026-09-15 신설).** 그전까지 문지기에는 **알림이 통째로
  없었다** — 정지가 걸려도 파일 하나 쓰고 로그 한 줄이 전부였고, 밤새 세션이
  자면 아침까지 아무도 몰랐다. 09-14 의 "로그에 CRITICAL 이 남았는데 아무도
  안 봤다"와 같은 형태다. 알리는 것 세 가지 —
  ① 🛑 **자동 정지**(낙폭·지갑·전고점·막는 것/안 막는 것·수동 재개 안내)
  ② 🔔 **재개 문턱 회복**(계좌당 **1시간 쿨다운** — 안 걸면 하루 1,440통)
  ③ 🚨 **조회 실패 5회 연속**(=5분, 낙폭 감시가 멈춘 상태) + ✅ 복구
  엔진과 **같은 발송기**(`lifecycle_live_signal_driver._telegram_notify`)를 쓴다 —
  두 벌 만들면 한쪽만 고쳐지는 날이 온다(교훈#102). 발송 실패는 전부 삼킨다.
  기동 로그에 `텔레그램 알림 — 배선 확인` 이 찍히는지 눈으로 본다.

### 매 사이클 보호 점검 (엔진 내장, 09-14 신설)

포지션 수 ≠ 조건부주문 수면 **즉시 손절 재등록 + 텔레그램**. 고아 주문은 드러내기만 한다.
정상이면 로그에 `보호 점검 — 보유 N건 전부 손절 걸림 ✅`.

> 09-14 12:10 에 1000SATSUSDT 가 **25분간 손절 없이** 열려 있었다. 로그에 CRITICAL 이
> 남았는데 아무도 안 봤다. **방어선은 로그가 아니라 매 사이클 대조다.**

---

### 🌙 야간 감시관 `night_watch.py` (2026-09-15 22:5x 정상화)

`pm2 kine-night-watch` · 런처 `night_watch.sh` · **cron `*/30` 발화 · `--no-autorestart`**
· `nice 15`(라이브 엔진 `KINE_NICE=0` 의 CPU 를 뺏지 않기 위해).
결정론이다 — **판단하지 않고 문턱에 걸리는지만 본다.** 주문은 내지 않는다.

알리는 것: 💥 네 스크립트 예외 · 🚨 무보호/고아 · 🚨 낙폭 −7% 경고선(계좌당 1h 쿨다운)
· 🔄 교체 조건 충족 · ⚠ 손절선 3% 이내 · 📋 30분 요약.

**교체 건의는 내용이 같으면 6시간 침묵한다.** 키는 「계좌→후보 이름」뿐 —
12h/24h 숫자를 키에 넣으면 30분마다 미세하게 움직여 쿨다운이 무의미해진다.

⚠ **2026-09-16 밤 — 문지기가 20번 죽었다.** `paper_slope()` 가 헤더도 없는
페이퍼 원장을 `pd.read_csv` 하다 `EmptyDataError` 를 냈고 잡히지 않아 프로세스가
통째로 내려갔다(pm2 재시작 24→44). 그 함수는 **파일 주석부터 "참고용. 판정에
쓰지 않는다"** 였다. **판정에 쓰지도 않는 장식값이 보호 루프를 죽이면 안 된다.**
보호 루프 안에서 부르는 참고용 계산은 전부 총함수여야 한다. 커밋 `9bc9a378`.

⚠ **문지기와 역할이 다르다.** 문지기는 −10%에서 **막는** 장치고, 감시관은 그
앞단에서 **사람을 깨우는** 장치다. 서로를 대체하지 않는다.

**배치했다고 도는 게 아니다** — 22:25 에 파일만 올려놓고 pm2·cron 어디에도
등록하지 않아, 내가 손으로 돌릴 때만 돌았다. 배치 뒤 `pm2 describe` 로
**status 와 cron restart 를 눈으로 확인**하라.

### 세션이 자면 무엇이 멈추나 (2026-09-15 확인)

| 살아 있는 것 (민트) | 멈추는 것 (이 세션) |
|---|---|
| 거래소 손절 조건부주문 (바이낸스 서버) | 정기 보고 |
| 매 사이클 보호 점검 + 텔레그램 | **교체 판정 `_rotate.py`** |
| 만기 청산 · 낙폭 문지기(60초) + 텔레그램 | 분위기 판정·원장 `_regime.py` |
| PM2 `autorestart=True` (3종 전부) | 사람 판단 |

⚠ **`_rotate.py`·`_regime.py`·`_accel.py`·`paper_ab.py` 는 crontab 에도 PM2 에도
없다.** 전부 사람이 손으로 돌린다 — 밤새 국면이 바뀌어도 **교체는 일어나지
않는다.** 보호는 자율이고 판단만 멈춘다.

### 중간 방어선 — 수동 정지 (2026-09-16 20:50 첫 사례)

트레일링 스톱(포지션 하나)과 낙폭 문지기(계좌 −10%) 사이에 **계좌 단위 중간
방어선이 없었다.** 계좌8 확인숏3 이 이 공백을 처음 메운 사례다.

**무엇을 보고 세웠나** — 순위도 문턱도 아니고 **국면 분할 측정**이다.
`_dir.py` 가 잡은 국면 전환 시각(17:40)을 경계로 계좌8 원장을 둘로 갈랐다.

| 구간 | 거래 | 실현합 | 자본% | 손절 |
|---|---|---|---|---|
| 11:37~17:40 (전환 전) | 5 | **+2.72%p** | +0.91% | — |
| 17:40~20:30 (전환 후) | 6 | **−14.85%p** | −4.95% | 7/11 = 64% |

같은 갈래·같은 계좌인데 국면이 바뀌자 부호가 뒤집혔다. 교체 조건 ①~⑨ 는
**갈래를 바꿀 근거**만 재지, "지금 이 갈래를 계속 돌릴 것인가"는 안 잰다.
그 사이를 사람이 메운다.

**정지가 무엇을 멈추고 무엇을 안 멈추나** — `HALT_ENTRY` 는 **신규 진입과
대기열만** 막는다. 손절·만기청산·트레일링 래칫·장부 대조·보호 점검은 계속 돈다.
그래서 **보유분은 방치되지 않는다.** 실제로 정지 뒤에도 계좌8 BTCDOMUSDT 는
래칫이 −5.00% → +0.12% 로 올린 잠금선에서 21:15 에 **플러스로 청산**됐다.

**해제는 언제** — ① 국면이 숏으로 되돌아오거나 ② `_regime.py` 분위기 판정이
통과하거나 ③ 롱에 맞는 갈래가 ⑥⑧⑨ 를 통과하면(이건 해제가 아니라 **교체**다).
⚠ **국면 판독은 최소 두 주기 연속 유지를 보고 바꾼다** — 09-16 18:13~19:13 에
30분 간격으로 두 번 뒤집은 적이 있다. 한 주기 반등으로 해제하지 마라.

### 트레일링 익절을 손절로 세지 않는다 (2026-09-16 22:15 · 대표님 승인)

`kine_trail.py` 가 손절선을 플러스 구간으로 올리면(래칫) 그 선에 걸린 청산도
**거래소에서 포지션이 사라지는** 모양은 손절과 똑같다. `kinematics_paper.py` 는
`exch_gone and not expired` 를 전부 `stopped=True` 로 찍고 있었다. 체결가는
`closing_fill` 로 정확히 받아 왔으므로 **손익은 맞았고 분류만 틀렸다.**

**왜 고쳐야 했나** — 손절율은 교체 조건 ④(24h 손절율 ≤ 45%)의 **입력**이고
`_rotate.py` 순위표의 「손절」 열이다. 그대로 두면 **래칫이 잘 돌수록 그 갈래가
교체 심사에서 불리해지는 역설**이 생긴다.

**고친 것** (`kinematics_paper.py`)

```python
trail = bool(exch_gone and not expired and net > 0)
...
"stopped": bool(stop_hit and not trail),
...
if broker is not None:
    row["trail"] = trail          # 실거래 원장에만 칸을 늘린다
```

⚠ **냉각(`_cd`)은 안 건드렸다.** 2026-09-12 에 만기 청산까지 넓힌 이유
("이익 뒤 재진입 5건 전패")는 트레일링 익절에도 그대로 적용된다 — **분류를
고치는 일이지 운용을 바꾸는 일이 아니다.**
⚠ 페이퍼는 `broker is None` 이라 `exch_gone` 이 항상 False → 이 갈래에 안 들어온다.
**대조군 원장은 스키마까지 그대로다**(체크리스트 8).

**소급 (`_backfill_trail.py`)** — 손절가 청산은 방향과 무관하게 −5% 이므로
**`net_pct > 0` 인 손절은 존재할 수 없다.** 만기는 처음부터 `stopped=False` 다.
그래서 `stopped and net_pct > 0` 인 행을 전부 뒤집었다. 손익·체결가는 안 건드렸다.

| 계좌 | 원장 | 손절 (전) | 손절 (후) | 트레일링 익절 | **손절율** |
|---|---|---|---|---|---|
| 계좌15 s6양다리 | 57건 | 20 | **7** | 13 | 35.1% → **12.3%** |
| 계좌8 확인숏3 | 13건 | 8 | **5** | 3 | 61.5% → **38.5%** |

⚠ **내가 "세 건"이라고 보고했는데 실제는 16 건이었다.** 정기 보고에서 눈에 띈
것만 셌기 때문이다. 원장 전체를 조건으로 훑기 전에는 건수를 말하지 마라.

⚠ 소급으로 **조건 ④가 느슨해지는 방향**으로 움직였다. 값을 보고 문턱을 옮긴 것이
아니라 **입력이 틀려 있던 것**이지만, 앞으로 ④로 떨어지는 갈래가 줄어든다는 뜻이다.

**남은 위험 — 사람이 손으로 플러스 청산한 것도 `trail` 로 찍힌다.** 만기 전
거래소 소멸 + 플러스면 구분이 안 된다. 수동 청산은 드물고 대개 의도된 익절이라
지금은 둔다.

### 🌙 야간 관리관 `night_gov.py` (2026-09-16 22:30 신설 · 대표님 지시)

**왜** — 이 세션은 절전으로 잠길 수 있다. 그 전까지 밤사이 자율은 문지기
(−10% 낙폭) 하나뿐이었고, 그 앞단의 판단은 전부 사람이 했다. 09-16 20:50 의
계좌8 정지가 그 예다 — **사람이 깨어 있어야만 가능한 방어**였다.

| | 하는 일 | 안 하는 일 |
|---|---|---|
| `night_gov.py` (pm2 `kine-night-gov`, cron `*/15`) | 계좌 **정지·재개 결정과 실행** | **주문을 내지 않는다.** 포지션·교체는 손대지 않는다 |
| `night_watch.py` (pm2 `kine-night-watch`, cron `*/30`) | 상태 요약 + 사건 알림 | 결정하지 않는다 |
| `kine_halt_gate.py` (pm2, 60초) | 낙폭 −10% 정지 / −3% 재개 | 갈래 성적을 안 본다 |

**되돌릴 수 있는 것만 자동으로 준다.** 정지는 폭발 반경이 없다 — 보유분의
손절·트레일링·만기 청산은 그대로 돈다. 교체는 강제 청산·레버리지·장부 이사가
따르므로 **관리관에게 주지 않았다**(조건이 차면 감시관이 알린다).

**판정 규칙**

| | 조건 (**연속 2회** 같은 판정이어야 움직인다) |
|---|---|
| 🛑 정지 | 페이퍼 3h·6h·12h **거래당이 전부 통행료(0.1004%) 미만** 且 12h 자본 ≤ **−5%** 且 실거래 24h 자본 < 0 |
| 🟢 재개 | 페이퍼 3h·6h **거래당이 통행료 초과** 且 12h 자본 ≥ **0%** |
| ⏸ 보류 | 창별 최소 체결 수(3h 3건 · 6h 6건 · 12h 10건) 미달 — **아무것도 안 한다** |
| ⏸ 보류 | 페이퍼 원장의 **마지막 체결이 120분 초과**로 오래됨 — 페이퍼가 죽으면 창 계산이 얼어붙은 과거를 읽어 멀쩡한 숫자를 낸다 |

**−5% 를 어디서 가져왔나** — 09-16 20:50 에 사람이 계좌8 을 세웠을 때
확인숏3 은 12h 자본 **−12.01%**, 같은 시각 세우지 않은 s6양다리는 **−1.98%**
였다. 즉 **부호가 아니라 크기가 둘을 갈랐다.** 문턱을 그 사이에 둔다.
22:21 예비비행에서 실제로 계좌8 만 「정지」, 계좌15 는 「유지」로 갈렸다.

**남의 정지는 풀지 않는다** — 파일 본문의 표식으로 가른다.
`야간 관리관 자동 정지`(제 것, 지울 수 있다) / `문지기 자동 정지`(문지기 것) /
표식 없음(사람이 건 것). 제 것이 아니면 재개 문턱을 넘어도 손대지 않는다.

**09-16 22:32 — 계좌8 의 사람 정지를 관리관에게 이관했다.** 세션이 자는 동안
확인숏3 이 회복하면 스스로 열리도록. 원본은 `/home/mint/bak/` 에 있다.

**남긴 것** — 매 회 판정과 근거가 `runs/night_gov.log` 에 쌓인다. 상태가 바뀔
때만 텔레그램이 울린다. 세션이 깨면 **이 로그부터 읽어라.**

### ⚠ 09-16 22:25 적발 — 감시관이 계좌8 을 하루 종일 안 보고 있었다

`night_watch.py` 의 `ACCTS` 가 `anti3s`(역확인3)로 굳어 있었다. 계좌8 은
11:37 에 `conf3s`(확인숏3)로 교체됐는데 **감시관 등록표만 안 따라갔다.**
그래서 밤새 요약에 계좌15 한 줄만 나갔고, 계좌8 의 무보호·낙폭·손절임박을
**아무도 안 보고 있었다.** 교훈#102(승격은 딸린 장치 전부의 이사)가 또 터졌다 —
교체 절차 4항의 「다섯 곳」에 `night_watch.py`·`night_gov.py` 를 더해 **일곱 곳**이다.
⚠ 목록을 믿지 말고 `grep -rn '<옛 원장 디렉터리>' /home/mint/auto_trading/backend` 로 훑어라.

### 최소 체결 수는 고정 상수가 아니라 갈래 구조에서 나온다 (2026-09-17 08:25)

09-16 밤 계좌8 이 44 주기 중 **21 회(48%) 판단 보류**였다. 나는 이것을
「새벽에 거래량이 줄어서」로 보고했다 — **틀렸다.** `_freq.py` 로 체결
**시각만** 재보니(성적은 한 칸도 안 읽는다):

| 갈래 | 슬롯/만기 | 시간당 청산 (실측 / 구조값) | 3h 보류 | 6h 보류 | 12h 보류 |
|---|---|---|---|---|---|
| s6양다리 | 6 / 4h | 1.57 / 1.50 | 10.0% | 0.0% | 0.0% |
| **확인숏3** | 3 / 4h | 0.91 / 0.75 | **41.1%** | **53.4%** | 14.7% |
| s10양다리 | 10 / 2h | 5.06 / 5.00 | 0.0% | 0.0% | 0.0% |
| 역확인3 | 3 / 4h | 0.91 / 0.75 | 41.4% | 51.1% | 15.8% |

**시각과 무관했다.** 확인숏3 의 가장 한산한 시각은 새벽이 아니라 **17시
(0.3건/h)** 였고 가장 바쁜 시각은 16시였다. 밤에만 보인 건 밤에만 봤기 때문이다.

**진짜 원인은 고정 상수다.** 갈래의 시간당 청산 수는 구조로 정해진다 —
`슬롯 수 ÷ 만기(시간)`. 실측이 구조값과 거의 일치한다(1.57/1.50 · 0.91/0.75 ·
5.06/5.00). 확인숏3 의 6h 창 기대 체결은 **4.5 건**인데 문턱을 **6 건**으로
뒀다. **기댓값보다 높은 문턱이니 절반 넘게 보류되는 것이 당연했다.**
슬롯이 적은 갈래를 구조적으로 벌주고 있었다.

**고친 것** — 문턱을 갈래마다 계산한다.

```python
def need(slots, hold_h, hours, frac):
    return max(2, int(round(frac * (slots / hold_h) * hours)))

MIN_FRAC_HALT   = 0.6    # 정지 판단에 필요한 표본
MIN_FRAC_RESUME = 1.0    # 재개는 더 두껍게 — 두 건 운 좋았다고 열지 않는다
```

| 갈래 | 3h | 6h | 12h | (구 문턱) |
|---|---|---|---|---|
| 계좌15 s6양다리 | 3 | 5 | 11 | 3 · 6 · 10 |
| 계좌8 확인숏3 | **2** | **3** | **5** | 3 · 6 · 10 |

재개 쪽은 따로 `MIN_FRAC_RESUME = 1.0` 을 요구한다(계좌15 3h 4건·6h 9건 /
계좌8 3h 2건·6h 4건). **비대칭은 의도다** — 닫힌 채로 두는 쪽이 안전하다.
표본이 모자라면 「재개」가 아니라 「유지」로 떨어뜨린다.

⚠ **문턱을 성적에서 유도하지 않았다.** `_freq.py` 는 `net_pct` 를 한 번도
읽지 않는다. 체결 시각만으로 유도한다 — 이것이 「값을 보고 조건을 고치지
않는다」를 지키는 방법이다.

⚠ `ACCTS` 에 **만기(시간)** 칸이 생겼다. 만기는 갈래 이름이 아니라 **엔진
설정**에서 온다 — 교체할 때 같이 고쳐라.

**결과** — 계좌8 이 「보류」에서 「유지」로 바뀌었다(정지는 그대로 유지된다).
이제 판단을 하고 나서 닫아 두는 것이지, 눈을 감고 닫아 두는 것이 아니다.

### 문지기 표기 정정 (같은 날)

문지기가 관리관이 건 정지를 `**사람이 건 정지**` 로 찍고 있었다. **안 푼 것은
옳았지만**(GATE_MARK 가 없으면 안 푼다) 로그를 읽는 사람이 누가 세웠는지
못 알아본다. `GOV_MARK` 를 알아보게 해 표기를 갈랐다 — **동작은 그대로다.**

```
🔒 계좌8 확인숏3 낙폭 -2.89% 로 재개 문턱을 넘었으나
   **야간 관리관이 건 정지**라 유지한다 — 관리관이 제 기준으로 푼다
```

## 5. 정기 보고

**에이전트는 스스로 깨어나지 못한다.** 매 보고 끝에 `ScheduleWakeup` 을 **다시 걸어야** 한다
— **질의 응대보다 먼저**. :01/:31 KST **경계 이후 +4분**에 올린다.
**고정 cron `*/30` 금지** — 봉 마감 전을 찍는다. **세션이 닫히면 멈춘다.**

②항 고정 블록(하나도 빼지 말 것):

1. 🚨 보호 점검 2. 자산 현황표(증감 병기) 3. 보유 포지션 손익표
   — 끝 두 줄은 **「다리 합」**과 **「자본 대비(합÷슬롯)」**로 **분리**(「합계」 한 줄 금지)
4. 🚦 문지기(지갑/전고점/낙폭/**정지문턱 지갑**) 5. **🔄 갈래 순위 + 교체 판정**
6. **🧭 국면 방향** (`_dir.py`, 2026-09-16 대표님 지시로 추가)
7. 🌡 분위기 + 📒 판정 원장 8. 손익 가속도 **전 갈래**(자르지 말 것)
9. 페이퍼 A/B 10. pm2 11. ③ 조치·건의

**⑥항 국면 방향을 왜 넣었나** — 2026-09-16 대표님 질문("지금 국면은 롱방향이야
숏방향이야"). 그때까지 보고에는 **시장 자체의 방향이 없었다.** 갈래 성적으로
역산하면 안 된다 — 갈래에는 선별·손절·슬롯이 섞여 있어 방향의 증거가 못 된다.
`_dir.py` 는 거래소 원본 1시간봉으로 **무조건 보유** 수익률을 잰다(유동성
게이트 24h 거래대금 ≥ 5M USDT, 약 271종목, 실행 16초).
**중앙값·상승비율을 주축으로 읽는다** — 평균은 몇 종목 폭등에 끌려간다.
중앙값 부호와 상승비율 50% 선이 **같은 쪽**을 가리켜야 방향이라 부르고,
중앙값이 통행료 0.1004% 보다 작으면 **방향이 있어도 못 먹는다**고 적는다.
BTC·ETH 단독도 같이 내 알트와 갈리는지 본다.

**정기 보고를 다른 보고로 갈음하지 마라.** 09-14 에 한 번 그랬다가 지적받았다.

**표는 마크다운 표로 낸다.** 대표님은 데스크톱 앱(Code 탭)으로 읽으신다 —
박스 문자(`┌ ─ │`)는 한글과 글자폭이 달라 **반드시 깨진다.** `report_box.py` 로
폭을 정확히 맞춰도 깨진다. 09-15 아침에 이걸 두 번 내고 두 번 지적받았다.
`report_box.py` 는 터미널·텔레그램·파일 산출물 전용이다.
정본: `.claude/references/rsi_report_format.md` 의 `## 표 문법 — 창구별`.

---

## 6. 함정 — 실제로 당한 것들

**`subprocess.run(shell=True)` 의 기본 셸은 `/bin/sh`(=dash)다** (2026-09-15 22:47).
dash 에는 `source` 가 없다. `source venv/bin/activate && python3 x.py` 가 **127 로
죽는다.** 야간 감시관이 네 진단 스크립트를 배치 이후 **한 번도 실행하지 못했는데**,
화면에는 `사건 1` 이라는 **숫자만** 찍혀 원인이 안 보였다 — 사건의 *내용*은
텔레그램으로만 나가고 콘솔에는 개수만 찍었기 때문이다.
**① 셸은 `executable="/bin/bash"` 로 못 박거나 venv 파이썬을 직접 불러라.
② 알림을 만들었으면 그 내용을 콘솔에도 찍어라 — 숫자만으로는 못 고친다.**
나는 이 사건을 처음에 "교체 건의였다, 감시관은 제 할 일을 했다"로 **오진했다.**
사건 개수를 보고 사건 내용을 추측했기 때문이다.


| 함정 | 내용 |
|---|---|
| `pkill -f "패턴"` | **내 ssh 명령줄까지 잡아 세션을 끊는다.** `pkill -f "[_]이름.py"` |
| ssh 120초 | 넘으면 백그라운드로 빠진다. **수집을 나눠 걸어라.** 빠지면 task 출력을 Read |
| 예약이 이르게 기동 | `ssh "sleep N; ..."` 을 `run_in_background: true` 로 걸어 경계를 넘긴다 |
| `pd.to_datetime` | **항상 `format="mixed"`** (수동 청산분이 마이크로초 서식) |
| 페이퍼 `net_pct` | **이미 통행료가 빠져 있다.** 또 빼면 이중 차감 |
| 갈래 이름 | **`paper_ab.py` 정본 등록표**로만 쓴다. 키를 잘못 붙여 오보한 적 있다 |
| 박스 문자 표 | 앱에서 **반드시 깨진다.** 정렬 문제가 아니라 문법 문제다 — 마크다운 표로 내라 |
| 겹친 창 기울기 | 3h/6h/12h/24h 는 **서로 겹친다**. 넷을 나란히 놓고 추세를 읽지 마라 — 비겹침 토막으로 재라 |
| 토막 격자 | 경계를 1시간만 밀어도 지속성 판정이 뒤집힌다. **격자 여러 개의 중앙값**으로 재라 |
| 낙관 전망 | 한두 주기 개선으로 재개·교체를 낙관하지 마라. **09-14 에 두 번 틀렸다** |
| 자본 누적 | 엔진은 **덧셈**이다(`equity += stake*net/100`). 곱셈으로 재계산하면 원장이 갈린다 |

---

## 7. 미결

- **기록 승인 대기**: §39~§48 · 교훈 **#112~#117**
  - #112 상수 하나가 회전수에 비례해 편향을 만든다
  - #113 메모리에만 둔 앵커는 재기동에 유실되고, 유실은 조용하다
  - #114 시각으로 못 가르는 것은 수량으로 가른다
  - #115 거래소가 거부해도 포지션은 열린 채 남는다
  - #116 손실 전략에 관문을 씌우면 무엇을 씌워도 좋아 보인다
  - #117 읽는 창이 신호 요구량보다 짧으면 조용히 매일 같은 시각에 장님이 된다
- **E-2 승격 기준 교체** — 현행 ④(자본% 격차 10%p)가 **표본 늘수록 나빠진다**.
  대안: 거래당 95% 하한 > 0 · 집중도 양수 · |t|<1.96 · 격차는 표본에 맞춰 조이는 구간
- **통행료 재검토 ②번** (§37-A 슬롯시간당 알파 1.73배 재계산)
- 손절/만기 분류 경계 재검토 (손절가 정확 체결인데 만기로 기록된 건 수)

**⛔ 대표님이 보류시킨 것** (먼저 꺼내지 말 것) — imp 선별 vs 무작위 대조 ·
imp 6년 파라미터 격자 · 요일축 아카이브 검정. 09-14 20:10 "조금 더 기다려 보고".

---

## 8. 이 트랙의 역사 (요약)

| 시점 | 일 |
|---|---|
| 09-07 | 속도저울 1군 승격(계좌15) |
| 09-13 | 충격240(계좌15) · 충격숏3(계좌8) — **둘 다 imp 계열** |
| **09-13** | **하루에 두 계좌가 함께 −45%.** 같은 계열이라 같이 무너졌다 |
| 09-14 | 낙폭 −17.7% 로 **전 계좌 정지**. 하루에 결함 4종 규명·수정 |
| 09-15 | **방향숏3(계좌15) · 역확인3(계좌8) 교체 + 재개.** 반대 선별, 상관 +0.084 |
| 09-15 | `tickbars.read_bars(days=2→3)` — imp 계열이 **매일 09:00~10:30 장님**이던 문제 해소 |

**09-14 에 잡은 결함 4종** — ① 수수료 상수 0.072 → **0.1004**(페이퍼가 회전수에 비례해
유리했다) ② `entry_ms` 유실로 수수료 최대 5.2배 과대 계상 ③ **지수 표기**(`str(1.1e-05)`)로
0.0001 미만 종목 손절 미등록 ④ 보호 점검 부재(노출 30분 → 5분)

**기각된 축** (다시 파지 말 것) — 기울기 관문 · 최근 N 평균 관문(6년 43,764거래, **p 0.8450**) ·
방향숏3 을 E-2 기준으로 본 교체(09-14 시점 판정, 09-15 국면에서는 채택됨)

---

## 9. 관련 문서

- 보고 형식 정본 — `.claude/references/rsi_report_format.md`
- 옛 인계문(폐기, 09-05 기준) — `.claude/references/rsi_track_handover.md`
- 메모리 — `project_kinematics_track_handover.md` · `feedback_dynamic_strategy_rotation.md` ·
  `feedback_report_to_ceo_format.md` · `feedback_total_pnl_is_the_metric.md`
- 옛 원장 보존 — `runs/kinematics_live/imp3s`(120거래) · `imp3s480`(17거래)
- 백업 — `/home/mint/kine.bak.*` · `gate.bak.*` · `tickbars.bak.*` · `bf.bak.*`
